name: githubissues
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.0-92.0.dev <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  http: ^1.2.0
  bloc: ^8.1.2
  freezed_annotation: ^2.4.1
  flutter_bloc: ^8.1.3
  flutter_screenutil: ^5.9.0
  injectable: ^2.3.2
  get_it: ^7.6.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0
  build_runner: ^2.4.8
  freezed: ^2.4.7
  json_serializable: ^6.1.5
  injectable_generator: ^2.4.1
flutter:
  uses-material-design: true
